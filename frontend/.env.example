# Base URL of the Flask backend (see ../requirements.txt / ../wsgi.py).
# In Render this is set automatically from the backend service — see
# the NEXT_PUBLIC_API_URL entry in ../render.yaml. Only needed here for
# local development.
NEXT_PUBLIC_API_URL=http://localhost:5000
