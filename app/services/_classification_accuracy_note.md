# Classification accuracy

The classifier uses deterministic phrase precedence, exclusions, confidence thresholds, and abstention. Validate changes against real catalog examples before broad backfill.
